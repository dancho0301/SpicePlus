%h1
  Listing genres

%table.table.table-hover
  %thead
    %tr
      %th
        Name
      %th
        Css class
      %th{colspan: "3"}

  %tbody
    - @genres.each do |genre|
      %tr
        %td
          = genre.name
        %td
          = genre.css_class
        %td.function
          = link_to '参照', admin_genre_path(genre), class: "btn btn-primary"
        %td.function
          = link_to '変更', edit_admin_genre_path(genre), class: "btn btn-primary"
        %td.function
          = link_to '削除', admin_genre_path(genre), method: :delete, data: { confirm: '本当によろしいですか？' }, class: "btn btn-danger"

%br

= link_to '新規作成', new_admin_genre_path, class: "btn btn-primary"
